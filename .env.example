# Server Configuration
PORT=3000

# Database Configuration
# For localhost development, these defaults are already set in server.js
# For production, set these in your hosting panel
DB_HOST=localhost
DB_PORT=3306
DB_NAME=u461595815_fpc
DB_USER=u461595815_fpcadmin
DB_PASS=f7McCB4#6IE!

# Frontend API URL (Optional)
# Only needed if you want to override the default API URL
# Development: http://localhost:3000
# Production: Leave empty to use same origin (server.js serves both frontend and API)
VITE_API_URL=